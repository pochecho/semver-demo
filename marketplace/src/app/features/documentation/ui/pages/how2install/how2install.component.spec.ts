import { ComponentFixture, TestBed } from '@angular/core/testing';

import { How2installComponent } from './how2install.component';

describe('How2installComponent', () => {
  let component: How2installComponent;
  let fixture: ComponentFixture<How2installComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ How2installComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(How2installComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should bre route', () => {
    
  });
});
